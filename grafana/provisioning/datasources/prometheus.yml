# ==================================================
# 프로메테우스에서 데이터 긁어오는 코드 있는 파일입니다.
# ==================================================
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true