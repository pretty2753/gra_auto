---
# - name: "목록 최신화를 위한 cache update"
#   dnf:
#     update_cache: yes
- name: Install common packages
  dnf:
    name:
      - vim
      # - git
      - python3
      - python3-pip
      - unzip
      - wget
      - tar
    state: present
